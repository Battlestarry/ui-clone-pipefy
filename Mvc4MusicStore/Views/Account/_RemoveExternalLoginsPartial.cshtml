@model ICollection<Mvc4MusicStore.Models.ExternalLogin>

@if (Model.Count > 0)
{
    <h3>Registered external logins</h3>
    <table>
        <tbody>
        @foreach (Mvc4MusicStore.Models.ExternalLogin externalLogin in Mode